---
interface Props {
  host: string;
  id: string;
}

const { host, id } = Astro.props;
---

<script
  defer
  is:inline
  src={`${host}/script.js`}
  data-website-id={id}
  data-domains="www.yuy1n.io,yuy1n.io"></script>
